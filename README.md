# git-user-random-gpg

Give the git user a random GPG every time the CI is run.

## Usage

```yaml
    - uses: iwishiwasaneagle/git-user-random-gpg@v1
      with:
        email: [INSERT EMAIL]
        name: [INSERT NAME]
```
